function solve(a, b, c) {
    let sumLength = a.length + b.length + c.length
    let avarrageLength = Math.floor(sumLength / 3)
    console.log(sumLength)
    console.log(avarrageLength)
}

solve('chocolate', 'ice cream', 'cake')
solve('pasta', '5', '22.3')
